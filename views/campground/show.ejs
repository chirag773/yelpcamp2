<% include ../partials/header %>
<h1><%= campground.name %></h1>

<img src="<%= campground.image %>" >

<h3><%= campground.description %></h3>

<% campground.comments.forEach(function(comment){ %>
 <p>
  <strong><%= campground.author %></strong> - <%= campground.text %>
 </p>
<% }); %>